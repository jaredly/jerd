{
    "private": true,
    "workspaces": [
        "language",
        "web"
    ],
    "scripts": {
        "prepare": "yarn --cwd language build",
        "web": "yarn --cwd web start",
        "test": "yarn --cwd language test",
        "tsc": "yarn --cwd language tsc -w",
        "watch": "yarn --cwd language watch-tests"
    }
}
