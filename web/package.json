{
    "name": "@jerd/web",
    "version": "1.0.0",
    "main": "index.js",
    "license": "MIT",
    "scripts": {
        "build": "esbuild src/run.tsx --bundle --define:process.env=\"{}\" --define:process.env.NODE_ENV=\\\"development\\\" --minify --sourcemap --platform=browser --outdir=.",
        "start": "esbuild src/run.tsx --bundle --define:process.env=\"{}\" --define:process.env.NODE_ENV=\\\"development\\\" --minify --sourcemap --platform=browser --servedir=. --outdir=. --serve=:4343"
    },
    "dependencies": {
        "@emotion/react": "^11.1.5",
        "@jerd/language": "1.0.0",
        "buffer": "^6.0.3",
        "react": "^17.0.1",
        "react-dom": "^17.0.1",
        "react-textarea-autosize": "^8.3.2"
    },
    "devDependencies": {
        "@types/react": "^17.0.3",
        "@types/react-dom": "^17.0.2",
        "esbuild": "0.10.1"
    }
}
