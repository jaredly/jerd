
(deffect fs
    (readFile (text) => text)
    (writeFile (text text) => text)
    )

(define xx 4)
(define y (+ xx (+ 3 4)))
(define z (lambda (a : int) (+ a xx)))
(define m (+ (z 1) 4))

(define getConfig
    (lambda ()
        (raise! (readFile "config.json"))
    )
)
